version: '2.1'
services:
  netspeed:
    image: firbykirby/netspeed
    ports:
      - "80"
    restart: always
  browser:
    image: balenablocks/browser
    network_mode: host
    privileged: true
    environment:
        - 'KIOSK=1'
  fbcp:
    image: balenablocks/fbcp
    privileged: true
